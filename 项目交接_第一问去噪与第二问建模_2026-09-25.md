# 项目交接：第一问去噪与第二问建模

> **历史交接记录。** 第二问已按后续决定只保留 V3：现行代码见 [`q2/README.md`](q2/README.md)，现行报告与输出见 [`q2_result/README.md`](q2_result/README.md)。下文提到的早期 Q2 路径与运行命令仅描述当时状态，相关旧版文件已清理，不再作为当前复现入口。第一问部分仍按本文原记录阅读。

交接日期：2026-09-25  
工作目录：`/home/cryus/code-project/C`  
范围：第一问新旧去噪方法审核、第二问训练折内去噪对照、机制模型与特征表示的证据边界。第三问不在本次交接范围内。

## 1. 接手后先了解的结论

**第二问继续以基础预处理作为主分析，新版去噪保留为敏感性对照。** 新版已通过严格嵌套时间分块接入第二问，四组实验已经运行并导出结果；机制特征的平衡准确率没有一组提高，一组持平、三组下降，不能以“波形更平滑”作为替换主方案的依据。

**新版修复了校正阶段直接使用测试方向标签的问题，但还不能称为完全无监督。** 候选策略选择仍使用训练方向标签。当前准确表述是“训练标签辅助选型、推断不使用方向标签”。只要所有选型都在训练折内，这与直接使用测试答案有本质区别。

**第二问已经有可运行模型、真实数据拟合、折外特征和分类结果，但尚未证明真实神经形成机制或稳定的左右可分性。** 现有模型可以提出可检验的机制假说并构造特征；原主模型四组置换检验均不显著，眼动负对照还能被该类特征完全识别，说明分类与神经机制特异性必须分别验证。

当前工作已经超过“只有建模方案”的阶段。接手者应先阅读下面的结果报告，再决定改动；部分历史讨论文档的完成状态已过时。

## 2. 优先阅读与文件入口

|用途|文件或目录|
|---|---|
|原始赛题|[服务于脑机接口与精神性疾病诊断的脑电图计算模型.docx](/home/cryus/code-project/C/服务于脑机接口与精神性疾病诊断的脑电图计算模型.docx)|
|第一问审核结论与指标解释|[Q1_unsupervised_audit/审核结论.md](/home/cryus/code-project/C/Q1_unsupervised_audit/审核结论.md)|
|第二问新版去噪对照结论|[Q2_fold_denoising_results/第二问去噪对照报告.md](/home/cryus/code-project/C/Q2_fold_denoising_results/第二问去噪对照报告.md)|
|第二问原主分析报告|[Q2_result/第二问实验报告.md](/home/cryus/code-project/C/Q2_result/第二问实验报告.md)|
|运行入口说明|[Q2/README_运行.md](/home/cryus/code-project/C/Q2/README_运行.md)|
|新版实验审计与完成状态|[verification.json](/home/cryus/code-project/C/Q2_fold_denoising_results/verification.json)、[manifest.json](/home/cryus/code-project/C/Q2_fold_denoising_results/manifest.json)|
|原主分析折外九维特征|`Q2_result/features/{A1,A2,B1,B2}_oof_nine_features.csv`|
|新版逐试次预测、指标与参数|`Q2_fold_denoising_results/predictions.csv`、`classification_summary.csv`、`paired_changes.csv`、`folds/`|

`Q2/第二题研究思路与模型方案.md` 和 `Q2/第二题讨论结论与当前完成状态.md` 保留了早期讨论，其中“未实现、未拟合”等状态不能代表当前进度。当前状态应以实际结果、运行清单及核验记录为准。

## 3. 数据约定与任务边界

- 四份输入为 `data/VisualCogA_Task-1.mat`、`VisualCogA_Task-2.mat`、`VisualCogB_Task-1.mat`、`VisualCogB_Task-2.mat`，分别简称 A1、A2、B1、B2。只有两名受试者，每组原始提示事件为 100 个，采样率为 256 Hz。
- 原始通道 1–3 为 Fz、F3、F4，是模型输入。通道 4–6 为机器处理结果，只能作辅助比较，不能作为干净脑电真值或训练目标。
- 使用 VisCue 平台起点对齐，方向编码为左 −1、右 +1。三角形的朝向不等于刺激出现在左右视野，不能直接套用对侧视觉通路解释。
- 分段窗口为 −250 至 796.875 ms，共 269 点。第一问主要分析 250–500 ms；第二问使用 50–250、250–500、500–750 ms 三阶段等权评价。
- 电位按原始数据单位报告，不擅自标注为 μV。±1000 附近饱和不可逆，不能把校正波形解释为恢复了被截断的真实信号。
- 缺少 EOG，只有三个额部电极；不能可靠排除方向相关眼动，也不足以唯一定位神经源。现有结果不支持人群泛化、临床诊断或真实神经因果机制的结论。
- 第二问所谓“原始数据方案”实际上包含基础滤波、基线校正及质量剔除，并非完全未处理的电压序列。

## 4. 第一问：已经查明的问题

### 4.1 标签依赖

旧文件为 `EEG_P300_artifact_correction_v7.py`，新文件为 `EEG_P300_artifact_correction_unsupervised.py`。

旧版按左右方向构建模板和均值修正，测试时用真实 Cue 选择。固定训练模型与 EEG，只翻转测试 Cue，旧版输出最大变化为 **92.2048 个原始电位单位**；新版四组共 20 折均为 **0**。因此旧版输出不能用于证明未知方向可解码。已知条件下的描述性 ERP 分析需要另行界定，并非所有旧结果一概失效。

新版合并了模板及均值修正，但 `select_candidate` 的背景选择、注入扫视方向和恢复损失仍利用训练标签。若继续追求“完全无监督”，必须一起改这些环节，不能只修改文件名或测试端接口。

### 4.2 效果不是全面改善

审核固定已保存的候选，重建两版模型及受控注入实验，保存输出与复算结果最大差异为 0；**没有重新执行完整连续滤波与全部候选搜索**。两版第一问有效试次数同为 90、95、94、92。

|数据组|统一逐折代理参考 MAE 降幅：旧→新|1 倍污染 NRMSE：污染输入→新版|新版相对污染输入降幅|
|---|---:|---:|---:|
|A1|11.22% → 12.16%|0.34660 → 0.33738|2.66%|
|A2|8.86% → 10.48%|0.72877 → 0.65193|10.54%|
|B1|7.39% → 7.22%|0.50352 → 0.46393|7.86%|
|B2|10.78% → 9.71%|0.65016 → 0.59413|8.62%|

统一 MAE 降幅仍是相对基础预处理、针对代理参考的指标，不是真值恢复率。新版四组 SNR 代理增量的既有 95% 区间全部跨零。在 12 个非零污染场景中，新版优于旧版的全波形 NRMSE 仅 4 个、左右差分 RMSE 仅 3 个；这些计数不是显著性检验。

A1 的 0.5 倍弱污染 NRMSE 从 0.17365 上升到 0.20884，存在过度修正。左右差分形状相关虽约为 0.988–0.998，幅度比仅约为 0.807–0.932；A1、B2 的前后半段差分相关仍为负。不能将高形状相关、幅度比或更接近原始 EEG 直接称为“视觉信息保留率”。

受控注入的恢复目标是注入前的实测背景，背景仍含原有伪影。真正已知神经信号的模拟、方向无关污染及零神经方向差异负对照仍需补强。

### 4.3 第一问待改但尚未改的内容

候选评分针对候选完整输出，最终实际输出却包含 15%/25% 混合及均值修正，优化目标与最终算子存在不一致。可优先让候选评估使用完整最终流程，再检查弱污染失真。

新版默认输出目录仍指向 `eeg_v7_results`，报告中还有旧 V7 命名和旧公式。运行时务必显式指定独立目录。`compare.py`、`compare_detailed.py` 直接比较不同评价参考的报表，不应作为最终效果依据。

## 5. 第二问：当前实现了什么

### 5.1 数据隔离与新版对照流程

`Q2/q2model/data.py` 从原始 MAT 建立 5 个独立时间块，各块分别进行 60 Hz 陷波、0.1–30 Hz 带通及基线处理，保留 24 秒边界缓冲；原主分析另有 32 秒缓冲敏感性分析。质量剔除采用固定规则，两分支共用试次。

新版有效试次数为 A1=67、A2=70、B1=69、B2=69，总计 275。这与第一问数量不同，源于独立时间块及缓冲要求，不能混用分母。

```text
原始 MAT → 独立时间块及固定质量剔除 → 外层留一时间块测试
  外层训练集：
    内层再留一块验证
    每个内层训练集独立拟合去噪模板、尺度、修正与候选策略
    选机制核数量、正则强度等参数
    在全部外层训练数据上重新拟合去噪、机制模型与分类器
  外层测试集：
    只用对应训练模型处理 EEG → 提取特征 → 预测
    最后才使用测试方向进行评分和描述
```

基础分支与新版分支使用完全相同的测试试次。新版入口不读取第一问预先导出的去噪 NPZ，也不会把随机折去噪结果直接交给另一套时间折分类。

### 5.2 模型、特征与机制解释

模型包含两层，交接时必须区分：

|层次|代码|实际含义|
|---|---|---|
|图像到神经群体再到头皮的示意模拟|`Q2/q2model/mechanism.py`|三角图像、空间方向响应、LGN 延迟、Wilson–Cowan 兴奋/抑制动态、突触与固定头皮投影；参数和几何主要为假设，未由真实 EEG 联合反演|
|真实 EEG 的有效响应拟合|`Q2/q2model/model.py`|空间模态与因果时间核的低维模型，拟合共同项和方向项，并生成单试次特征|

有效响应表达式为：

```text
x̂_d(t) = B(A + dD)h(t)，d ∈ {−1,+1}
Δ右−左(t) = 2BDh(t)
```

三个正交空间模态为共同模态 `(Fz+F3+F4)/√3`、中线模态 `(2Fz−F3−F4)/√6`、侧化模态 `(F3−F4)/√2`。训练内选择 2 或 3 个时间核、正则强度，并进行有界优化。

特征提取将单试次 EEG 投影到训练得到的空间和时间基底，测试端不需要方向标签。主模型按所选核数产生 **6 或 9 维**特征；另有固定三核九维导出，以及“三空间模态 × 三时间窗”的九维对照。不能把所有主分类折统称为固定九维。

分类器为训练内标准化的收缩 LDA。图像示意模拟和实测有效响应拟合尚未形成一个经数据验证的联合机制模型；估计的方向项也不自动证明方向差异由特定神经群体产生。非线性去噪尚未显式进入神经观测方程，去噪后的拟合参数尤其只能视为有效响应参数。

## 6. 第二问关键结果及允许的结论

### 6.1 同试次、同时间块的新旧输入对照

|组别|试次|基础 BA|新版 BA|变化（百分点）|基础 AUC|新版 AUC|
|---|---:|---:|---:|---:|---:|---:|
|A1|67|47.99%|47.99%|0.00|0.484|0.487|
|A2|70|57.57%|54.63%|−2.94|0.529|0.505|
|B1|69|51.01%|49.54%|−1.47|0.468|0.472|
|B2|69|46.41%|46.20%|−0.21|0.481|0.479|

BA 为左右召回率平均。对同一时间块成对重采样得到的 BA 差值 95% 区间依次为 `[0,0]`、`[−6.55,0]`、`[−4.05,0]`、`[−5.17,+4.29]` 个百分点。仅有 5 个时间块，且模型固定，区间不覆盖重新训练、开发调参或跨受试者不确定性；A1 的零区间只是两分支类别预测一致。

新版相对各自测试 ERP 的差分拟合 RMSE 四组均降低，但针对共同基础预处理目标的 RMSE 仅小幅变化，A1 还略升。因此“拟合去噪后的平滑对象更容易”不能转写为“辨别能力提高”或“神经信号恢复更准”。

### 6.2 原主分析的显著性和机制局限

原 `Q2_result/validation/permutation_tests.csv` 每组包含 999 次完整监督流程置换：

|组别|原主模型 BA|原始 p|Holm 校正 p|
|---|---:|---:|---:|
|A1|47.99%|0.658|1.000|
|A2|57.57%|0.157|0.628|
|B1|51.01%|0.415|1.000|
|B2|46.41%|0.713|1.000|

这些 p 值属于原主分析，**不能移用给新版去噪分支**。新版尚未重跑完整置换、完整参数重估、跨受试者及前向预测验证。

原主分析另有以下需要保留的反证：

- 48 个方向/通道/阶段拟合条目中，22 个 R² 为负，不能统称拟合良好。
- `Q2_result/synthetic/scenario_metrics.csv` 中，只有方向相关眼动、没有神经方向差异的场景 BA=1.0。这证明该特征表示缺少神经特异性，不证明实测信号全部来自眼动。
- 探索性侧化特征在 A2、B1 上的 BA 约为 63.25%、64.03%，但 A1、B2 仅约为 46.56%、37.46%。它是后续假说，不能按外层成绩挑选后直接宣布改进成功。

可用的论文表述是：“构建了具有生理启发的有效响应模型及无测试标签输入的特征表示，提出左右响应差异的候选解释；当前样本尚未获得稳定高于随机的识别证据，也不足以唯一验证真实神经形成机制。”

## 7. 本轮新增实现与核验

|文件|职责|
|---|---|
|`Q1_unsupervised_audit/audit.py`|第一问固定策略复算、测试标签翻转、统一参考与受控污染指标审核|
|`Q2/q2model/fold_denoising.py`|训练折内去噪适配、内外层独立拟合、训练数据缓存与编号审计|
|`Q2/run_q2_fold_denoising.py`|四组双分支完整网格对照、预测、参数、配对区间与清单导出|
|`Q2/report_fold_denoising.py`|指标独立复算、保存模型概率重建、哈希审计与图表报告|
|`tests/test_q2_fold_denoising.py`|6 项针对嵌套隔离、标签不变性、基线等价等行为的测试|

已完成四组、五外折、两分支，共 **40 次外层模型拟合**，保存了逐折去噪策略、内层训练/验证编号、模型参数和预测。基础分支与归档原主分析的试次、类别预测完全一致，概率最大差异为 0。

已通过 **19 项相关测试**：新增 6 项、第一问回归 5 项、第二问核心 8 项。没有声称当前工作区全部历史测试通过。核验还覆盖输入/源码/输出哈希、相同测试试次、折内编号隔离、指标与分类概率复算。

`classification_comparison.png` 与 `waveform_and_features.png` 已实际查看最终图，视觉核验记录保存在 `verification.json`。测试和运行日志位于 `Q2_fold_denoising_results/audit/`。

## 8. 复现方式

当前可用科学计算环境为 `/home/cryus/code-project/ML/.venv/bin/python`。不要默认系统 `python3` 已安装全部依赖；`Q2/requirements-q2.txt` 可作为环境准备参考。

先阅读已有结果；若要完整重跑对照，使用新输出目录，避免修改当前归档：

```bash
cd /home/cryus/code-project/C
MPLCONFIGDIR=/tmp/q2-handoff-mpl OPENBLAS_NUM_THREADS=1 /home/cryus/code-project/ML/.venv/bin/python Q2/run_q2_fold_denoising.py --output Q2_fold_denoising_results_recheck
MPLCONFIGDIR=/tmp/q2-handoff-mpl OPENBLAS_NUM_THREADS=1 /home/cryus/code-project/ML/.venv/bin/python Q2/report_fold_denoising.py --output Q2_fold_denoising_results_recheck
MPLCONFIGDIR=/tmp/q2-handoff-mpl OPENBLAS_NUM_THREADS=1 /home/cryus/code-project/ML/.venv/bin/python -m unittest discover -s tests -p test_q2_fold_denoising.py -v
```

上述命令为完整网格对照，不等于原主入口的所有扩展验证。只调试时使用 `--quick` 和另一个输出目录；不得以快速运行替代正式实验。

已有目录默认拒绝覆盖。`--resume` 核验源文件、输入和配置一致后重算，并不是逐折跳过已完成任务的断点续跑。源码有改动时应新建结果目录，不能篡改清单来绕过签名检查。

报告脚本可以单独重建报告，但会更新核验文件；重新生成图后必须实际检查图像，再记录视觉核验，不能照抄此前的通过状态。

## 9. 接手时不要误用的文件或结果

1. 根目录 `run_q2_unsupervised.py`、`run_q2_svm_test.py` 及相关 `Q2_unsupervised_results*` 先读取第一问去噪数组，再采用不同划分分类，不能替代当前训练折内对照作为严格独立评估。
2. 不能把 `folds/` 的折外处理波形拼接后，作为“已经无泄露的数据集”用于任意新划分。改变下游划分，就要在相应训练折重新拟合去噪和全部选型步骤。
3. 当前工作区 `Q2/q2model/validation.py` 有既存改动：`classify_svm` 实际调用 LDA 的 `classify`，`mechanism_svm_fused` 名称不能作为真实 SVM 实验的证据。新的双分支入口明确使用 LDA。
4. 原 `Q2_result` 是归档实验；当前 `validation.py` 已与归档源码不同，不能假设重跑旧入口必然得到同一实验。保留旧清单，修改后的模型另开输出目录。
5. 当前有大量未提交文件和既有 Q1 图片、验证代码改动。本轮没有提交 Git，也没有重置这些改动。继续开发前先看 `git status --short`，不要批量清理未跟踪结果或覆盖他人修改。

## 10. 下一步工作优先级

### 优先级一：补上第二问的机制证据链

明确哪些参数只是有效响应参数，哪些能由现有数据约束。将示意的图像/神经群体响应与实测模型建立可检验连接，并比较共同响应、方向响应、眼动混杂等替代模型。只有在折外预测、负对照及参数稳定性上得到支持，才逐步增强机制解释。三通道和两名受试者的限制不能靠更复杂模型消除。

### 优先级二：围绕可区分性改进特征，并严格重新验证

可检查侧化模态、分阶段特征及个体差异。当前以波形拟合选核，未必最有利于分类，这是待验证假说。任何特征筛选或分类目标调参都必须放入内层训练，外层测试只作一次评价；不能按已查看的四组外层分数挑选最佳方案后报告无偏性能。

选定候选后，对完整监督流程重跑标签置换，并加入眼动方向独立、反向关联、纯眼动和无方向信息负对照。已有两名受试者可作有限跨个体检查，但不能据此宣称人群泛化。

### 优先级三：优化去噪而不预设一定有增益

先统一候选损失与最终混合输出，针对弱污染和零新增污染约束失真。若要求完全无监督，移除选型中的方向标签依赖，并采用不依赖条件标签的恢复目标。优化后同时评价已知真值的波形、左右差分、F3−F4 恢复误差和严格折外解码，不能只选择单一改善指标。

### 完成标准

- 数据来源、剔除规则、试次编号、内外层划分和所有训练参数可追溯；同试次比较，测试端不依赖标签。
- 原始输入、代码和结果清单匹配；改动后的实验另存，旧 p 值和旧核验记录不移植。
- 改善结论同时说明误差、区间、负对照与失败场景；区分描述性结构保留、可解码性和机制特异性。
- 代码、实际执行结果、可复算指标及最终检查过的图表同时交付；未执行的验证明确列为待办。

下一位接手者可以直接从第 2 节的两份审核报告和第 7 节代码开始，无需重建已经完成的基础对照。当前最需要补强的是机制与可区分性的证据，而不是再次包装已有平滑效果。
