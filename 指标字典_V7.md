# V7 指标字典与比较口径

数值来自同一批通过质量筛查的试次；每组按“左/右 × Fz/F3/F4”六行计算，再等权汇总。`预处理、V3、V4、V6、V7` 使用相同折、训练参考、测试试次和窗；缺失指标只在五阶段共同有效行中配对，不补零。原始数据未确认物理标定，因此幅度写“原始电位单位”，不擅自写 µV。V3/V4 的旧报告若用了不同参考或剔除集，不能与本表直接相减。

| 指标 | 定义与窗口 | 解释 |
|---|---|---|
| `baseline_RMS` | 刺激前 −250–0 ms 单试次 RMS，再对试次平均；原始单位 | 振幅描述，降低不等于去噪成功 |
| `trial_PTP_median` | 完整试次各试次最大减最小，再取中位数；原始单位 | 极差描述，过小也可能是信号损失 |
| `edge_band_ratio` | Welch 功率谱在 1–3 Hz 加 20–30 Hz 的积分，除以 1–30 Hz 总积分 | 边带能量比例，包含可能的真实神经成分 |
| `SNR_proxy_dB` | `10log10(mean(ERP²)/mean((trial−ERP)²))`，0–500 ms | ERP 相对试次间残差的代理比；残差也含真实变异，越高仅表示该定义下更突出 |
| `MAE`、`RMSE` | ERP 与外折训练同方向代理模板的绝对误差均值、平方误差均方根，250–500 ms；原始单位 | 越小越接近代理参考，不等于越接近无噪声真值 |
| `corr`、`shape_distance` | 250–500 ms 的 Pearson 相关，以及逐序列 z 标准化后平均绝对差 | 描述相对代理模板的形状；常数序列记缺失 |
| `P300_positive_mean`、`P300_AUC_unit_ms` | 250–500 ms 对正部 `max(ERP,0)` 求平均或梯形积分 | 描述额区正向响应；大小无单一优劣方向 |
| `P300_amp`、`P300_lat_ms` | 窗内最大正值及其时间 | 端点正值也可能出现，不能单凭此项叫典型 P300 |
| `positive_peak_valid` | 250–500 ms 内是否存在满足局部峰规则的正峰 | 缺失时不填 0 ms；与 `P300_amp` 是否有正值分开解释 |
| `amp_error`、`latency_error_ms`、`P300_positive_mean_error`、`P300_AUC_error_unit_ms` | 各正向特征与代理参考差的绝对值 | 依赖代理参考；峰缺失时峰值/潜伏期误差记缺失 |
| `retention_ratio` | `norm(处理后差分)/norm(预处理差分)`，250–500 ms | 差分包括左减右 ERP、各方向 F3 减 F4；接近 1 不能证明保留的是神经成分 |
| `waveform_correlation` | 处理前后差分波形 Pearson 相关 | 处理前可能有方向相关眼动；高相关不等于神经真值 |
| `normalized_RMSE` | 半合成完整试次恢复 RMSE / 注入前背景 RMS | 已知注入只验证对额外人工污染的恢复；0 倍时非零误差表示算法改动背景 |
| `fit_RMSE` | 固定 50 ms 结点三次样条与 ERP 的同样本残差，报告 50–750 ms 和 250–500 ms | 描述曲线平滑拟合程度，不是预测泛化误差 |
| `proxy_MAE_reduction_pct`、`SNR_proxy_gain_dB` | 六个方向×通道行等权组均值相对预处理的变化 | 百分比与 dB 各自独立；正点估计不等于稳定增益 |
| `contrast_correlation` | 原始事件编号前后对半后，各半独立计算左右 ERP 差分，在 250–500 ms 对三通道拼接波形或单通道求 Pearson 相关 | 检查方向差异的时段重复性；不能区分神经视觉响应与方向相关眼动 |

外折训练参考是该折训练集每个方向低伪影半数试次的逐点中位数。指标文件按测试试次对应参考后进行同方向平均。代理参考参与模型构造，因此存在方法相关性；参考不是独立清洁 EEG。逐方向逐通道区间用 400 次、组均值区间用 800 次分方向试次重采样；同一批索引配对比较各阶段。区间固定已选模型、剔除集与训练代理参考，只反映这批试次的有限样本波动，不代表跨时段、跨受试者或开发调参不确定性。时段稳定性表另存每半段的左右试次数。

图表中灰蓝虚线代表预处理，浅紫点线代表 V6，青色实线代表 V7。ERP 阴影为逐时间点试次重采样区间，不是整条曲线的同时置信带。热力图“完整色域”与“细节视图”在各阶段共用色标，细节视图显式标出截色比例。半合成图的四组纵轴各自归一到各自背景 RMS，因此不要跨组把数值当作相同绝对电位误差。汇总图的 MAE、SNR、保留比分列独立轴，点旁横线是组均值 95% 重采样区间，不能合成单一分数。
