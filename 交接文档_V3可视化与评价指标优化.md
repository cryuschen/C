# V3 脑电可视化与评价指标优化交接文档

## 1. 本轮任务目标

下一轮工作的重点是改善图中呈现的真实信号质量、评价指标的可信度，以及两者之间的解释一致性。先固定数据、试次集合和评价口径，再比较任何算法或参数调整。不能通过改变坐标轴、挑选有利试次、改变分母或隐藏不利指标来制造“提升”。

当前只完成交接，没有修改去噪算法、指标计算或图片数据，也没有重新运行模型。

## 2. 题目与数据边界

- 赛题原文：`服务于脑机接口与精神性疾病诊断的脑电图计算模型.docx`。当前需求补充：`/home/cryus/桌面/可视化图表与评价指标.wps`。
- 四组输入：`data/VisualCogA_Task-1.mat`、`data/VisualCogA_Task-2.mat`、`data/VisualCogB_Task-1.mat`、`data/VisualCogB_Task-2.mat`。A/B 为受试者，Task-1/2 为两个实验项目；每组目前提取到 100 个视觉提示事件，采样率 256 Hz。
- 只用 Fz、F3、F4 原始记录通道（数据行 0–2）；VisCue 在数据行 7，−1 为左提示，+1 为右提示。赛题要求避免直接采用机器自带处理后的 FzDecon、F3Decon、F4Decon 作为建模输入。
- 当前试次窗口为提示前 250 ms 至提示后约 800 ms；正向响应分析窗为 250–500 ms。只有额区三个电极，图和结论不能据此宣称观测到中央或顶区 P300 的空间分布。源数据缺少可靠电位单位说明，沿用“原始电位单位”，不要改标为 µV。

## 3. 当前工程状态

- 主程序：`EEG_P300_artifact_correction_v3.py`；依赖清单：`requirements-v3.txt`。去噪主干包括连续信号预处理、坏试次筛查、分方向低伪影模板、五折折外校正、ERP 拟合和半合成注入测试。
- 最近一次代码与结果提交：`fc3d06f`。此后用户要求把**现有结果文件**改成中文名并按受试者、项目归档；这些文件移动目前尚未提交。先检查 `git status`，不要把大量删除与新增误判为数据丢失。
- 当前目录 `eeg_v3_results/` 共 36 个文件，其中 27 张图片。四个 `受试者X_项目Y/` 文件夹各有 6 图 + 1 张逐方向逐通道评价表；`跨项目对比/` 有 2 图；`半合成验证/` 有 1 图和 1 CSV；`汇总与说明/` 有 3 CSV 和说明文档。
- 关键汇总：`eeg_v3_results/汇总与说明/第一问评价指标汇总.csv`；空间差分：`eeg_v3_results/汇总与说明/左右刺激与额区空间差异指标.csv`；拟合参数：`eeg_v3_results/汇总与说明/三通道ERP拟合参数.csv`；指标定义：`eeg_v3_results/汇总与说明/第三版结果说明.md`。
- **输出路径尚未同步**：当前脚本重新运行会在 `eeg_v3_results/` 根目录写出英文文件名，不能自动写入中文分类文件夹。下一轮若需要复算，应先决定输出到独立实验目录还是更新脚本的命名与归档逻辑，避免把旧结果和新结果混在一起。此次交接不做复算。

## 4. 已知结果与不能忽略的反例

下表为当前版本内部的“V3 前 → V3 后”；V3 前已做 60 Hz 陷波、0.1–30 Hz 带通及基线校正，并非未经处理的原始 EEG。MAE、相关和潜伏期误差均针对低伪影代理参考，不能等同真实无噪声波形误差。表中每组为六个方向 × 通道行的均值。

| 数据集 | 可用试次 | MAE | 波形相关 | 代理 SNR dB | 潜伏期误差 ms | 左右差分幅度比¹ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| A 项目一 | 90/100 | 38.38 → 5.17 | 0.487 → 0.524 | −12.04 → −11.92 | 18.2 → 38.3 | 0.47 |
| A 项目二 | 95/100 | 91.46 → 22.85 | **0.685 → 0.450** | **−4.39 → −7.54** | 15.0 → 67.1 | 0.40 |
| B 项目一 | 94/100 | 25.61 → 11.99 | **0.372 → 0.360** | −20.88 → −16.65 | 165.4 → 89.8 | 0.52 |
| B 项目二 | 92/100 | 91.28 → 6.97 | −0.199 → 0.253 | −8.76 → −8.59 | 83.6 → 74.2 | 0.35 |

¹ 对 Fz、F3、F4 各自计算 250–500 ms 左减右 ERP 差分的“V3 后范数 / V3 前范数”，再取三通道均值。小于 1 表示当前差分幅度变小；它不能单独判定究竟去除了伪影还是抹掉了神经特征。

结论：目前只能说部分噪声和代理参考误差下降，**不能宣称去噪与视觉特征保留都已改善**。A 项目二尤需优先核查。上一版 v3 与当前汇总使用不同的试次范围和参考排除规则，不能直接比较 CSV 数字来证明算法提升；核心去噪函数在上次提交中没有改变。

## 5. 已确认的评价与绘图问题

1. **参考不是真值。** `build_clean_reference_model` 用低伪影试次生成模板；当前误差表排除了进入该模板的评价试次，但模板仍来自同一数据集，且校正训练折与代理参考间仍可能相关。应区分“代理参考误差”和半合成的已知注入真值误差。
2. **峰值/潜伏期均值可能不是配对比较。** 无正峰时值为空；例如 A 项目一六行中，去噪前峰值和潜伏期各缺 3 行，去噪后各缺 1 行；B 项目一去噪前缺 3 行、去噪后缺 0 行。现有汇总分别跳过空值取均值，前后可能由不同的方向 × 通道组成。必须报告有效行数，并在同一组配对有效行上比较误差；无峰比例本身也要报告。
3. **部分图混合左右刺激。** `plot_three_channel_erp_fitting` 与 `plot_task_comparison` 汇合两个方向；如果左右响应不同，平均可能相互抵消。下一轮至少提供分方向图或明确的等权条件均值，并写清各方向试次数。
4. **“原始”与“V3 前”不是同一级处理。** 典型试次图比较基线对齐的原始通道与“预处理 + V3”；ERP 和热力图比较已经滤波的 V3 前与 V3 后。图注和文件说明要固定这三个阶段的名称，最好在关键图同时显示原始、预处理后和 V3 后，以区分滤波收益和 V3 校正收益。
5. **拟合成功不等于有可信的 P300 成分。** 参数表 12 个通道拟合均标为 `gaussian`，但其中 5 个 P300 拟合幅度小于 0.5 原始单位；B 项目二 F3 的 R² 约 0.157。若参数贴近幅度下界、潜伏期上界或整体拟合差，应标记为“不支持可靠 P300 参数解释”，不能把数值收敛当成生理发现。
6. **频谱与 SNR 名称需要约束解释。** 当前 `edge_band_ratio` 计算 1–3 Hz 与 20–30 Hz 占 1–30 Hz 的比例，两个频段并非纯噪声；`SNR_proxy_dB` 的残差也包含真实试次变异。两者可作为描述量，不能单独作为“噪声被去掉且信号保留”的证明。
7. **半合成场景覆盖有限。** 现有注入测试以 A 项目一的校正试次为背景，只验证一种人工眨眼/扫视形态与固定随机种子。其好结果不能推广到四组真实数据或其他伪影形态。
8. **汇总权重和分母需固定。** 当前每组把六个方向 × 通道行等权平均；左、右试次数并不总相等。下一轮必须说明是等权条件平均、按试次数加权，还是逐试次汇总，并始终显示样本数和剔除数。

## 6. 建议执行顺序

### 第一阶段：冻结可比较基线

- 保存当前四组逐通道 CSV、空间差分 CSV、拟合参数、图片清单及输入数据哈希作为基线；候选实验输出到独立目录，不覆盖现有中文结果。
- 固定数据拆分、坏试次规则、刺激方向、窗口、参考模板构建方式和随机种子。所有候选方法在**同一批可用试次**上比较，不再用旧版不同统计口径的数字作改善证据。
- 核对 `.mat` 的标签、事件上升沿、边界试次和饱和数。当前严重坏试次规则依赖原始单位下的固定阈值（999、1800、600），需要给出数据分布与敏感性检查，再决定是否调整。

### 第二阶段：修正指标口径

- 建立指标字典：公式、时间窗、通道、刺激方向、单位、分母、缺失条件、期望方向、是否依赖代理参考、是否能用于跨版本比较。
- 对峰值、潜伏期和相关等可能缺失的指标，先保存逐方向逐通道有效性标记，再做配对前后比较；同时报告正峰检出率。不要把缺失值当作 0，也不要让前后样本集合悄悄变化。
- 将主要结论分成三个维度：噪声/伪影下降；ERP 波形及正向响应保留；左右刺激与 F3−F4 差异保留。任何一维变差都需显式报告。参考模板只称代理参考；半合成测试单独报告已知注入真值误差。
- 对试次重采样给出每组、每方向的区间估计及样本数。只有两名受试者，不将试次置信区间解释为人群层面的泛化精度。

### 第三阶段：定位需要优化的信号处理环节

- 先复核 A 项目二的相关、形状距离、SNR 与左右差分，再看 B 项目二的左右差分；画出固定试次的原始、滤波后、V3 后及模板波形。查清性能变化是在滤波、模板选择、双分量门控、回归系数还是坏试次筛查中出现。
- 仅在预先固定的训练数据上调整候选参数。逐项消融垂直分量、水平分量、门控阈值、回归上限和基线处理，记录每一步对去噪及差分保留的影响；保留原 v3 作为对照。
- 扩展半合成注入到四组背景、多个伪影幅度/时刻及预设随机种子，分别报告恢复误差、P300 正向特征和左右差分的分布。避免只展示最有利的一次注入。

### 第四阶段：重绘可解释的图

- 典型试次预先定义选择规则并标注原始试次编号；前后图使用可比坐标范围。需要显示极端值时另给局部放大图，不靠截断纵轴强化改善观感。
- 左右刺激、Fz/F3/F4、项目一/二保持相同符号、颜色与差分方向（左减右、F3 减 F4）。跨任务图说明方向混合或采用等权条件平均。
- 热力图在同一数据集前后使用共享色标，注明色标裁剪分位；ERP 置信带分方向计算并标明是试次重采样，不要混成受试者置信区间。
- 拟合图同时展示拟合残差或至少展示 R²、边界状态和“无可靠正峰”标记；只有通过预先定义的质量规则后，才在图上标注 P300 峰值和潜伏期。

## 7. 下一轮完成标准

1. 四组数据的试次总数、剔除数、参考数与评价数能逐项对账；每张图和每行指标都能追溯到同一明确的试次集合。
2. 新旧方法在相同输入、相同窗口、相同参考和相同配对规则下比较；输出同时包含改善项和退步项，不用单个 MAE 或半合成结果概括总体表现。
3. P300 峰值、潜伏期的缺失与拟合边界显式可见；所有汇总写明单位、方向、有效样本数和区间估计。无正峰不得编造潜伏期。
4. 所有图的图例、颜色、刺激方向、参考/原始/预处理/V3 阶段和坐标范围前后一致；逐图检查是否有遮挡、裁剪或文字误导。
5. 候选结果保存在独立目录，旧结果和原始数据不被覆盖；最终确认后再统一更新中文命名与分类规则，并验证完整脚本能重复生成同一套结果。

## 8. 给下一位执行者的第一步

先阅读赛题第一问与上述两份需求文件，打开 A 项目二的 `左右刺激三通道ERP对比图`、`P300时间窗放大图` 和逐方向指标 CSV；用固定试次重算相关、形状距离与左右差分，确认变差发生在哪一阶段。随后再动算法参数或重新设计汇总图表。当前仓库存在尚未提交的中文重命名与目录整理，不要直接把 `git status` 中的文件移动当作模型结果变化。
