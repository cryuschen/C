# 第二问结果

当前结果从[采用 Q1 数据的新模型报告](第二问_Q1数据级联机制与判别模型.md)开始阅读；[运行代码](../q2/README.md)通过 `q2/main.py` 一次执行。新输出统一位于 `q1_model/`。

新模型使用 Q1 的可复核波形结果：`v7` 用于已知左右条件的机制拟合，`before` 用于不读取测试 Cue 的方向判别。它提供 LGN 到皮层再到头皮的级联计算模型、左右差异解释、24 维特征、逐试次留出预测和置换检验。

`docs/` 和 `results/` 中的 V3 报告与结果保留为历史复核材料，不再是当前主入口的产出。
