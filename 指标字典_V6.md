# V6 指标字典

所有真实数据指标沿用原始电位单位，不标注 µV。`before` 为预处理，`after` 为当前行 `stage` 指定的预处理/V3/V4/V6。输入为同一批可用试次，按方向×通道计算。四阶段汇总为六行等权，缺失项仅在四阶段共同有效行上配对；不将缺失记零。

| 指标 | 公式/操作 | 窗口与分母 | 单位 | 解释或期望方向 | 依赖代理参考 |
|---|---|---|---|---|---|
| baseline_RMS | 每试次 sqrt(mean(x²)) 后对试次平均 | −250≤t<0 ms，当前方向全部可用试次 | 原始单位 | 波动描述，降低不必然改善 | 否 |
| trial_PTP_median | median_trials(max_time(x)−min_time(x)) | 整个试次 | 原始单位 | 极差描述，非越小越好 | 否 |
| SNR_proxy_dB | 10log10(mean(ERP²)/mean((trial−ERP)²)) | 0–500 ms，当前方向全部可用试次 | dB | 数值较大表示ERP更突出；残差含真实变异 | 否 |
| edge_band_ratio | (积分PSD[1,3]+积分PSD[20,30])/积分PSD[1,30] | Welch，完整试次，均值PSD | 比值 | 频谱描述，不是纯噪声比例 | 否 |
| MAE | mean(abs(ERP−reference)) | 250–500 ms，65个采样点 | 原始单位 | 小代表更接近代理参考 | 是 |
| RMSE | sqrt(mean((ERP−reference)²)) | 同上 | 原始单位 | 小代表更接近代理参考 | 是 |
| corr | Pearson(ERP,reference) | 同上 | 无量纲 | 大代表形状接近；任一方常数则缺失 | 是 |
| shape_distance | mean(abs(z(ERP)−z(reference))) | 同上 | 无量纲 | 小代表形状接近；常数则缺失 | 是 |
| P300_positive_mean | mean(max(ERP,0)) | 同上 | 原始单位 | 大小本身不是优劣 | 否 |
| P300_AUC_unit_ms | 梯形积分 max(ERP,0) dt | 250–500 ms | 原始单位·ms | 大小本身不是优劣 | 否 |
| P300_amp | max(ERP)，仅最大值>0时有效 | 同上 | 原始单位 | 正峰不等于显著P300 | 否 |
| P300_lat_ms | argmax ERP的采样时间，仅正峰存在时有效 | 同上；采样间隔3.90625 ms | ms | 无越大越好方向；端点最大也记录 | 否 |
| amp_error | abs(peak−reference_peak) | 同上；任一正峰缺失则缺失 | 原始单位 | 小代表更接近代理参考 | 是 |
| latency_error_ms | abs(latency−reference_latency) | 同上；任一正峰缺失则缺失 | ms | 小代表更接近代理参考 | 是 |
| positive_mean_error / AUC_error | abs(feature−reference_feature) | 同上 | 原始单位 / 原始单位·ms | 小代表更接近代理参考 | 是 |
| retention_ratio | norm(差分_after)/norm(差分_before) | 250–500 ms；左减右每通道，F3减F4每方向 | 比值 | 接近1仅表示幅度改变小；原信号也可能含伪影 | 否 |
| waveform_correlation | Pearson(差分_before,差分_after) | 同上 | 无量纲 | 大仅表示校正前后形状相近 | 否 |
| trial_retention | 可用试次/全部事件 | 每组全部100事件 | % | 本次各方法共用剔除集，不用于区分算法 | 否 |

V3内部原函数分母含极小常数避免除零；本组数据空间差分范数非零。`positive_peak_valid`是每阶段每方向×通道的标记。`reference_positive_peak_valid`表示该行代理参考是否存在正峰。`paired_rows`是四阶段共同有效行数，`valid_rows`是某阶段自身有效行数，两者不可混用。

## 参考与验证边界

真实参考为对应训练折中每方向低伪影半数试次的逐点中位数；按测试方向内试次等权平均各自训练模板。每折训练参考编号保存在各组“逐折训练参考评价清单.csv”。模板与校正相关，不是独立无噪声真值。不能与旧版“剔除全局参考试次”的汇总数字直接比较。

半合成验证以污染前预处理实测试次为恢复目标，比较完整窗口的RMSE、左减右ERP在250–500 ms的RMSE、各方向×通道正向均值误差及有效配对潜伏期误差。normalized_RMSE以背景全试次RMS归一化。选型objective=(RMSE+0.5×左右恢复RMSE+正向均值误差)/背景RMS。训练内零污染权重0.5、两个非零幅度各0.25。V6在所选V4输出和预处理之间固定使用15%校正量；该比例经过当前四组和测试场景的开发观察，不能当作独立调参验证。测试场景按数据集×折×种子等权；同一背景的重复场景并非独立受试者。

## 图表设计与来源

波形、窗内放大、固定试次、差分和拟合使用折线，因为横轴为连续时间且任务要求比较形状；热力图呈现试次间幅度分布；半合成图比较预设污染强度。颜色固定：预处理灰蓝虚线、V3橙色点划线、V4紫色点线、V6青色实线、代理参考黑色点线。所有源波形来自每组`可复核波形.npz`，典型试次编号单独保存。热力图同时提供四阶段共享完整色域图及共享98%绝对值分位的细节图；细节图标注截色比例。ERP置信带是固定模型下的试次逐点区间，不是全曲线同时置信带。

连续滤波为固定参数离线双向滤波，不能将这套结果宣称为实时预测能力。随机试次五折不等于跨受试者或跨时段验证。训练评分的MAD归一化在各自训练子集内部重新计算，固定坏试次筛查和典型试次评分保持原口径。

## V6 补充说明

V6 的已知人工注入验证使用0、0.5、1、2倍强度，四组各五外折、三个种子。归一化RMSE为完整试次恢复RMSE除以污染前预处理背景RMS；0倍注入时未校正误差为0，校正后非零值是背景改动成本。样条拟合采用50–750 ms内固定50 ms结点的三次最小二乘样条，报告该范围和250–500 ms的拟合残差RMSE；只有250–500 ms内存在满足局部峰规则的正峰才报告拟合峰潜伏期。端点最大正值可以进入旧版 `P300_amp` 描述量，但不算V6的合格局部正峰。所有区间是固定模型与固定代理参考下的试次重采样区间，不能当作跨受试者不确定性。
