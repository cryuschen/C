# 第一问 V7 终版提交说明

建议先打开 `output/pdf/第一问_脑电预处理与视觉响应建模_终稿.pdf` 阅读图文论证；详细方法、五阶段指标、逐通道结果与局限见 `eeg_v7_results/第一问终稿实验报告.md`。图像原图和数值 CSV 位于 `eeg_v7_results/`；公式口径见 `指标字典_V7.md`。

核心源程序是 `EEG_P300_artifact_correction_v7.py`，需与 `EEG_P300_artifact_correction_v3.py`、`EEG_P300_artifact_correction_v4.py`、`data/` 放在同一目录。安装 `requirements-v4.txt` 后运行：

```bash
python EEG_P300_artifact_correction_v7.py --output eeg_v7_results
python -m unittest discover -s tests -v
```

图文 PDF 为现成提交稿；全套 36 张 PNG 与 35 份 CSV、四组可复核 NPZ 保存在结果目录。`运行清单.json` 保存源程序、输入文件和数值 CSV 的 SHA256。提交包只保留第一问最终采用的 V7 结果与其必需的 V3/V4 依赖，没有把探索阶段的 V5/V6 结果混入。

结果是四组开发数据的折外分析，并非独立受试者验证。MAE 使用训练折代理参考，SNR 是 ERP/试次残差代理量，左右差分保留比相对处理前；这些指标不能单独证明眼动去除或神经源无损恢复。正峰只作为额区描述，不声称中央/顶区典型 P300 或临床诊断性能。
