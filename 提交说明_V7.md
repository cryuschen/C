# 第一问 V7 终版提交说明

建议先阅读 `eeg_v7_results/第一问终稿实验报告.md`，再查看该目录的图表、逐通道结果、半合成验证和运行清单。公式与限制见 `指标字典_V7.md`。结果只展示原始／预处理与 V7 的前后对照，以及半合成实验的未校正基准。

核心源程序是 `EEG_P300_artifact_correction_v7.py`，运行时需要同目录 `data/` 中的四份原始 MAT 数据和 `requirements-v7.txt` 所列第三方库。运行：

```bash
python EEG_P300_artifact_correction_v7.py --output eeg_v7_results
python -m unittest discover -s tests -v
```

全套 44 张 PNG、逐方向指标 CSV 和四组可复核 NPZ 位于结果目录；`四组核心指标重采样区间.csv` 与 `左右差分前后时段稳定性.csv` 用来核对组均值区间和前后半段波形是否重复。`运行清单.json` 保存 V7 源程序、输入文件和数值 CSV 的 SHA256。历史版本文件已从当前工作树移除，可通过 Git 历史找回。

结果是四组开发数据的折外分析，并非独立受试者验证。代理 MAE 降幅的组均值重采样区间均高于零，但 SNR 代理增量区间全部跨零；A 项目一和 B 项目二的左右差分前后半段相关为负。MAE 使用训练折代理参考，SNR 是 ERP/试次残差代理量，左右差分保留比相对处理前；这些指标不能单独证明眼动去除或神经源无损恢复。正峰只作为额区描述，不声称中央/顶区典型 P300 或临床诊断性能。
