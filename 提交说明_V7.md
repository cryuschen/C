# 第一问 V7 终版提交说明

建议先打开 `output/pdf/第一问_脑电预处理与视觉响应建模_终稿.pdf` 阅读图文论证；详细方法、五阶段指标、逐通道结果与局限见 `eeg_v7_results/第一问终稿实验报告.md`。图像原图和数值 CSV 位于 `eeg_v7_results/`；公式口径见 `指标字典_V7.md`。

核心源程序是 `EEG_P300_artifact_correction_v7.py`，运行时只需同目录的 `data/` 四份原始 MAT 数据和 `requirements-v4.txt` 所列第三方库，不需要 V3、V4 代码文件或独立的三栏分解绘图脚本。运行：

```bash
python EEG_P300_artifact_correction_v7.py --output eeg_v7_results
python -m unittest discover -s tests -v
```

图文 PDF 为现成提交稿，可用 `build_first_question_pdf.py` 和 `requirements-report.txt` 在有 Noto Sans SC 字体的环境中重建；全套 44 张 PNG（含新增各组三栏伪影分解图）与 37 份 CSV、四组可复核 NPZ 保存在结果目录。新增 `四组核心指标重采样区间.csv` 与 `左右差分前后时段稳定性.csv`，用来核对组均值区间和前后半段波形是否重复。`运行清单.json` 保存 V7 源程序、输入文件和数值 CSV 的 SHA256。提交 V7 时无需打包 V3/V4；旧版代码仍留在仓库供历史版本复现。

结果是四组开发数据的折外分析，并非独立受试者验证。代理 MAE 降幅的组均值重采样区间均高于零，但 SNR 代理增量区间全部跨零；A 项目一和 B 项目二的左右差分前后半段相关为负。MAE 使用训练折代理参考，SNR 是 ERP/试次残差代理量，左右差分保留比相对处理前；这些指标不能单独证明眼动去除或神经源无损恢复。正峰只作为额区描述，不声称中央/顶区典型 P300 或临床诊断性能。
